version: "3.8"

# ============================================================================
# Shared PostgreSQL — used by all BI tools for testing
# ============================================================================

services:
  bi-postgres:
    image: postgres:16
    container_name: bi_postgres
    environment:
      POSTGRES_DB: analytics
      POSTGRES_USER: analyst
      POSTGRES_PASSWORD: analyst_pass
    ports:
      - "5439:5432"
    volumes:
      - bi_postgres_data:/var/lib/postgresql/data
      - ./sample-data.sql:/docker-entrypoint-initdb.d/01-sample-data.sql
    command: >
      postgres
      -c shared_preload_libraries='pg_stat_statements'
      -c pg_stat_statements.track=all
      -c max_connections=200
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U analyst -d analytics"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - bi-network

volumes:
  bi_postgres_data:

networks:
  bi-network:
    name: bi-network
    driver: bridge
